# Security Notice

This repository contains an exported n8n workflow for demonstration purposes.

⚠️ Before using in production:

- Remove all credentials
- Replace Telegram bot tokens
- Replace Google Sheets OAuth credentials
- Replace spreadsheet IDs
- Review webhook URLs

Never commit:
- API keys
- OAuth tokens
- .env files
- Production secrets

Always configure credentials locally inside your n8n instance.
